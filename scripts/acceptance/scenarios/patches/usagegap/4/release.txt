交接台 v5：分享链接的按钮文案改成实际范围（团队内可见）。
- v4：交接导入的第二步不再把合法的 CSV 判成不支持的格式。
- v3：归档看板的空状态给出下一步。
- v2：把「批量导出」的入口放进首页，不再只能从归档看板页脚进入。
- 使用数据依据：sharelink 的 copyMatchesBehaviour=false；改文案不改行为，链接范围一直是团队内。
- 税务报表（taxreport）没有改动：它使用率低是因为目标用户访谈里没有人要求过（askedFor=false），
  这是一条待验证的判断，不是缺陷。
- 其余功能页与 /usage 端点不变。
