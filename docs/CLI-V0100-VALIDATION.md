# 0.10.0 CLI 接入验收

验收日期：2026-09-10。此次修改切换执行入口，不改变项目目标、统一看板或发布确认机制。

## 已验证

- `npm run typecheck`、`npm test`、`npm run test:ui`、`npm run build:app` 通过。完整服务测试 148 项通过、2 项可选集成跳过；界面测试 165 项通过。最终历史上下文兼容调整另跑原生对话 26 项测试通过。
- 用本机 `codex-cli 0.153.4` 和隔离 CODEX_HOME、本地模型响应夹具执行独立集成测试：CLI 新建、流式回复、同轮追加、冷恢复原任务，以及写入锁下 fork 接续均通过。没有调用真实付费模型或在用户项目内执行工具。
- CLI 缺失、启动退出、初始化超时、发送中退出、明确拒绝、中文跨字节分块、未知接续不重复创建、保留旧运行及预算等有自动测试覆盖。
- 实际安装版 Morrow 0.10.0 的 CLI 路径为 `/Users/bytedance/.local/bin/codex`；API 返回 connected=true，具备新建、恢复、发送、停止与答复能力。运行时窗口通过 CUA 看到 CLI 已连接与实际版本。
- 真实 JustCV 旧频道遇到 App writer lock 后，已通过 CLI 原生 fork 接续。旧任务 `01a08596-4029-74e2-ab93-2c613c2e5d82` 保留，新 CLI 任务为 `01a0898e-6f32-7cf3-91ef-4765167da405`。真实 API 返回 connected=true、80 条最新历史及继续分页信息。
- 对比升级前 SQLite 在线备份：5 个项目、8 个 feature、52 条运行的记录逐项不变；9 个频道仅 JustCV 的当前 sessionId 改为新 CLI ID。绑定和迁移审计保存新旧任务关系；继承历史不重复记为新运行。
- 撤销了 Morrow 自有 CODEX_CLI_PATH 启动覆盖及桥接启用标记。原 Codex App、桥接和原后台 PID 92763/92790/92792 及其启动时间均保持不变。没有终止或重启它们。

## 验证边界

最终接续提示的安装版窗口检查遇到 Mac 锁屏，尚待解锁复核；该提示及发送界面已通过 UI 自动测试。真实 CLI 集成的模型输出来自本地夹具，不代表生产模型、账号配额或业务效果已验证。旧 App 历史只在接续时继承，后续不再双端实时同步。其他旧频道按需接续，未为了验收启动其自动工作。
