/**
 * The work records under the name the rest of the app imports them by. Each page lives in its
 * own file — `FeatureWork.tsx`, `ProjectThinking.tsx`, `ProjectReleases.tsx` over
 * `work-shared.tsx` — and this barrel keeps every existing import path working.
 */
export { FeatureWork } from './FeatureWork';
export { ProjectThinking } from './ProjectThinking';
export { ProjectReleases } from './ProjectReleases';
